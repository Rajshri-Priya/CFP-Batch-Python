# define a function that takes in two arguments and returns their sum
def add(x, y):
    return x + y

# create a lambda function that does the same thing
add_lambda = lambda x, y: x + y
print(add_lambda(5, 10)) 
